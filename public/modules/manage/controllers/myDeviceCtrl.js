angular.module('manage')
    .controller('myDeviceController', [
        '$scope',
        '$route',
        '$location',
        '$routeParams',
        '$state',
        function($scope, $route, $location, $routeParams, $state) {

        }
    ]);
